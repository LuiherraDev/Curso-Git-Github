name: ⛰️ Acceso a la Fortaleza Prohibida

on: 
  workflow_dispatch:

jobs:
  acceso-a-la-base:
    runs-on: ubuntu-latest

    steps:
      - name: El agente llega a la Fortaleza
        run: |
          echo "Agente ${{github.actor}} llega a la fortaleza"
          echo ""
          echo "Buscar los archivos de la legión"
          ls -la
          echo ""
          echo "❌ pero qué pasó? no hay nada"
      - name: "Joker desbloquea la bóveda"
        uses: actions/checkout@v4
      
      - name: "Ahora si, vamos alla!"
        run: |
          echo "Buscar los archivos de la legión"
          ls -la
          echo "👌 Funciona"
          echo ""
          echo "Planes:"
          ls planes/
          echo "Villanos:"
          ls miembros/
          echo "Inteligencia:"
          ls inteligencia/




          